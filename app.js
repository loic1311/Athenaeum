
(function(){
'use strict';const RELEASE='1.2.1-test';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const S=AthStore;
let editing=null,selectedAvatar='bust',selectedApps=[],pendingUnlock=null,pendingLaunch=null,pendingPage='home',onboardingStep=0,onboardingPid=null;
function assetAvatar(a){return './assets/avatars/'+a+'.svg'}
function assetLogo(a){return './assets/logos/'+a+'.svg'}
function openModal(id){$('#'+id).classList.add('open')}function closeModal(id){$('#'+id).classList.remove('open')}
function setProfileLocked(locked){
  const gate=$('#profileGate'),shell=$('#mainShell');
  document.documentElement.classList.toggle('profile-locked',!!locked);
  document.body.classList.toggle('profile-locked',!!locked);
  if(gate){
    gate.classList.toggle('hidden',!locked);
    gate.setAttribute('aria-hidden',locked?'false':'true');
  }
  if(shell){
    shell.classList.toggle('hidden',!!locked);
    shell.toggleAttribute('hidden',!!locked);
    shell.toggleAttribute('inert',!!locked);
    shell.setAttribute('aria-hidden',locked?'true':'false');
  }
}
function clearProtectedView(){
  ['home','apps','library','progress','settings'].forEach(n=>{const el=$('#page-'+n);if(el)el.innerHTML=''});
  if($('#pageTitle'))$('#pageTitle').textContent='Athenaeum';
  if($('#pageSubtitle'))$('#pageSubtitle').textContent='Selecteer eerst een profiel';
  if($('#sideUser'))$('#sideUser').innerHTML='';
  if($('#sideApps'))$('#sideApps').innerHTML='';
}
function requireProfile(){
  const p=current();
  if(!p){clearProtectedView();setProfileLocked(true);return null}
  return p;
}
function renderProfiles(){const ps=S.loadProfiles();const g=$('#profileGrid');g.innerHTML=ps.map(p=>`<button class="profile-card" data-profile="${p.id}"><img src="${assetAvatar(p.avatar)}"><strong>${esc(p.name)}</strong><small>${p.pin_hash?'🔒 PIN':'zonder PIN'}</small>${appBadges(p)}</button>`).join('')+`<button class="profile-card profile-add" id="addProfile"><b>＋</b><strong>Nieuw profiel</strong><small>eigen apps, data en voortgang</small></button>`;$$('[data-profile]').forEach(b=>b.onclick=()=>chooseProfile(b.dataset.profile));$('#addProfile').onclick=()=>editProfile(null)}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function appMeta(name){return name==='scriptorium'?{name:'Scriptorium',logo:assetLogo('scriptorium'),desc:'Academisch onderzoek, corpus, primaire bronnen, theorie en training.'}:{name:'Paideia',logo:assetLogo('paideia'),desc:'Dagelijks leren, kennisnetwerken, examens, streaks en herhaling.'}}
function allowedApps(p=current()){return p?S.profileApps(p):[]}
function hasApp(name,p=current()){return allowedApps(p).includes(name)}
function appBadges(p){const a=allowedApps(p);return a.length?`<div class="profile-app-badges">${a.map(x=>`<span>${esc(appMeta(x).name)}</span>`).join('')}</div>`:'<div class="profile-app-badges"><span>nog geen apps</span></div>'}

function editProfile(id){editing=id;const p=S.loadProfiles().find(x=>x.id===id);selectedAvatar=p?.avatar||'bust';selectedApps=p?allowedApps(p):[];$('#profileModalTitle').textContent=p?'Accountinstellingen':'Nieuw profiel';$('#profileName').value=p?.name||'';$('#profilePin').value='';$('#deleteProfileBtn').classList.toggle('hidden',!p);$('#removePinBtn').classList.toggle('hidden',!p?.pin_hash);$('#avatarChoices').innerHTML=S.AVATARS.map(a=>`<button class="avatar-choice ${a===selectedAvatar?'active':''}" data-avatar="${a}" title="${a}"><img src="${assetAvatar(a)}"></button>`).join('');$$('[data-avatar]').forEach(b=>b.onclick=()=>{selectedAvatar=b.dataset.avatar;$$('[data-avatar]').forEach(x=>x.classList.toggle('active',x.dataset.avatar===selectedAvatar))});const access=$('#appAccessChoices');if(access)access.innerHTML=S.APPS.map(a=>{const m=appMeta(a);return `<label class="app-access-choice"><input type="checkbox" data-app-access="${a}" ${selectedApps.includes(a)?'checked':''}><img src="${m.logo}"><span><strong>${m.name}</strong><small>${m.desc}</small></span></label>`}).join('');$$('[data-app-access]').forEach(c=>c.onchange=()=>{selectedApps=$$('[data-app-access]:checked').map(x=>x.dataset.appAccess)});openModal('profileModal')}
async function saveProfile(){const name=$('#profileName').value.trim();if(!name)return S.toast('Geef een naam.');const pin=$('#profilePin').value.trim();selectedApps=$$('[data-app-access]:checked').map(x=>x.dataset.appAccess);let p;if(editing){const patch={name,avatar:selectedAvatar,apps:selectedApps};if(pin)patch.pin=pin;p=await S.updateProfile(editing,patch)}else p=await S.createProfile({name,avatar:selectedAvatar,pin,apps:selectedApps});closeModal('profileModal');renderProfiles();if(editing){renderShell();showPage('apps')}else{enterProfile(p.id);setTimeout(()=>startOnboarding(p.id,true),220)}}
async function chooseProfile(id){const p=S.loadProfiles().find(x=>x.id===id);if(!p)return;if(p.pin_hash){pendingUnlock=id;$('#unlockPin').value='';openModal('pinModal');setTimeout(()=>$('#unlockPin').focus(),80)}else enterProfile(id)}
async function unlock(){const p=S.loadProfiles().find(x=>x.id===pendingUnlock);if(!p)return;const ok=await S.verifyPin(p,$('#unlockPin').value);if(!ok)return S.toast('Onjuiste PIN.');closeModal('pinModal');enterProfile(p.id)}
function enterProfile(id){
  S.setCurrentProfile(id);
  sessionStorage.setItem('athenaeum_unlocked_'+id,'1');
  setProfileLocked(false);
  renderShell();
  showPage(pendingPage||'home');
  AthSync.startAuto(id);
  const target=pendingLaunch;pendingLaunch=null;pendingPage='home';
  if(target&&hasApp(target,current()))setTimeout(()=>openApp(target),180)
}
function leaveProfile(){
  AthSync.stopAuto?.();
  const active=S.currentProfile();
  if(active?.id)sessionStorage.removeItem('athenaeum_unlocked_'+active.id);
  S.setCurrentProfile('');
  clearProtectedView();
  setProfileLocked(true);
  renderProfiles();
  window.scrollTo({top:0,behavior:'auto'});
}
function current(){return S.currentProfile()}
function avatar(p){return assetAvatar(p.avatar)}
function renderShell(){const p=current();if(!p)return leaveProfile();$('#sideUser').innerHTML=`<img src="${avatar(p)}"><div class="user-copy"><strong>${esc(p.name)}</strong><small>${p.pin_hash?'PIN beveiligd':'profiel actief'}</small></div><div class="side-user-actions"><button id="sideAccount">Account</button><button id="sideSwitch">Wissel</button></div>`;const sa=$('#sideApps');if(sa)sa.innerHTML=allowedApps(p).map(a=>{const m=appMeta(a);return `<button class="side-app-link" data-side-app="${a}"><img src="${m.logo}"><span>${m.name}</span></button>`}).join('')||'<div class="tiny" style="padding:6px 10px;color:#8aa39c">Geen apps toegewezen</div>';$$('[data-side-app]').forEach(b=>b.onclick=()=>openApp(b.dataset.sideApp));$('#sideAccount')?.addEventListener('click',()=>editProfile(p.id));$('#sideSwitch')?.addEventListener('click',leaveProfile);const s1=$('#sheetScriptorium'),s2=$('#sheetPaideia');if(s1)s1.classList.toggle('hidden',!hasApp('scriptorium',p));if(s2)s2.classList.toggle('hidden',!hasApp('paideia',p));updateMode()}
function openApp(name){const p=current();if(!p)return;if(!hasApp(name,p))return S.toast(`${appMeta(name).name} is niet toegewezen aan dit profiel.`);const url=name==='scriptorium'?`./apps/scriptorium/index.html?ath_profile=${encodeURIComponent(p.id)}`:`./apps/paideia/index.html?ath_profile=${encodeURIComponent(p.id)}`;location.href=url}
function todayISO(){const d=new Date(),off=d.getTimezoneOffset()*60000;return new Date(d-off).toISOString().slice(0,10)}
function parentPaideiaToday(st){const lp=st.learning_paths||{},cfg={max_daily_minutes:60,light_every:3,...(st.plan_config||{})},dow=new Date().getDay(),labels={ancient:['🏺','Oude Geschiedenis'],early_modern:['🕯️','Vroegmoderne Tijd'],general_history:['🌍','Algemene Geschiedenis'],general_knowledge:['🧠','Algemene Kennis'],pharmacy:['⚗️','Farmacie'],skill:['🛠️','Algemene Vaardigheden']};const active=Object.entries(lp).filter(([k,v])=>v?.active&&labels[k]&&(Array.isArray(v.study_days)?v.study_days.map(Number).includes(dow):true));const rank={high:0,normal:1,light:2};active.sort((a,b)=>(rank[a[1].priority]??1)-(rank[b[1].priority]??1));let due=active.filter(([,v])=>v.priority!=='light');const light=active.filter(([,v])=>v.priority==='light'),every=Math.max(1,Number(cfg.light_every||3));if(light.length&&(st.daily_count||0)%every===0)due.push(light[Math.floor((st.daily_count||0)/every)%light.length]);if(!due.length&&active.length)due=[active[0]];const cap=Math.max(10,Number(cfg.max_daily_minutes||60));let used=0;due=due.filter(([,v],i)=>{const m=Math.max(5,Number(v.minutes||10));if(i===0){used=m;return true}if(used+m>cap)return false;used+=m;return true});return due.map(([k,v])=>`${labels[k][0]} <strong>${esc(labels[k][1])}</strong> — ${Number(v.minutes||10)} min`).join('<br>')||'Geen automatische Paideia-sessie gepland vandaag.'}
function parentExamSummary(st){const e=st.exam_config||{},w={enabled:true,day:0,weight:40,...(e.weekly||{})},m={enabled:true,day_of_month:28,weight:60,...(e.monthly||{})},days=['zondag','maandag','dinsdag','woensdag','donderdag','vrijdag','zaterdag'];return `${w.enabled?`📅 Weekexamen: <strong>${days[Number(w.day)]||'gepland'}</strong> · ${w.weight}%`:'📅 Weekexamen: uit'}<br>${m.enabled?`🧠 Mastery: <strong>dag ${m.day_of_month}</strong> van de maand · ${m.weight}%`:'🧠 Mastery: uit'}<br>🧊 IJsblokje vanaf ${Number(e.freeze_threshold||75)}% bij volledig beoordeeld examen.`}
function renderHome(){const p=requireProfile();if(!p)return;const st=S.getProfileData(p.id,'paideia_state',{streak:0,icecubes:p.icecubes??2,last_completed:'',weekly_scores:[],monthly_scores:[],mastery:{}}),sync=AthSync.cfg(p.id),apps=allowedApps(p);const quick=apps.length?apps.map(a=>{const m=appMeta(a),action=a==='paideia'?'Start sessie':'Open werkruimte';return`<div class="quick-app"><img src="${m.logo}"><div class="grow"><h3>${m.name}</h3><p>${m.desc}</p></div><button class="btn ${a==='paideia'?'gold':'primary'}" data-app="${a}">${action}</button></div>`}).join(''):`<div class="no-apps">Aan dit profiel zijn nog geen apps gekoppeld. Open <strong>Account</strong> om toegang toe te wijzen.</div>`;const paideia=hasApp('paideia',p)?`<div class="card"><h3>Vandaag volgens jouw Paideia-plan</h3><p>${parentPaideiaToday(st)}</p><p class="tiny">Theorie verschijnt in Paideia vóór de normale oefeningen.</p></div><div class="card"><h3>Jouw evaluaties</h3><p>${parentExamSummary(st)}</p></div>`:`<div class="card"><h3>Jouw Athenaeum</h3><p class="muted">Home blijft bewust compact: hier start je je toegewezen apps en zie je platformstatus. In <strong>Mijn apps</strong> staan de inhoudelijke voortgang en appdetails.</p></div><div class="card"><h3>Profieltoegang</h3><p>${apps.length?apps.map(a=>`✓ ${appMeta(a).name}`).join('<br>'):'Nog geen apps gekoppeld.'}</p></div>`;$('#page-home').innerHTML=`<div class="hero slide-up"><div class="spread"><div><div class="chip">ATHENAEUM V1.2.1 · TESTBUILD</div><h2>Welkom terug, ${esc(p.name)}</h2><p>Je persoonlijke ingang naar onderzoek en leren. Alleen jouw apps, planning en voortgang verschijnen hier.</p></div><img src="${avatar(p)}" style="width:82px;height:82px;border-radius:50%"></div></div><div class="stat-grid"><div class="stat"><div class="k">🔥 Streak</div><div class="v">${hasApp('paideia',p)?(st.streak||0)+' dagen':'—'}</div></div><div class="stat"><div class="k">🧊 IJsblokjes</div><div class="v">${hasApp('paideia',p)?(st.icecubes??2):'—'}</div></div><div class="stat"><div class="k">Jouw apps</div><div class="v">${apps.length}</div></div><div class="stat"><div class="k">Cloudsync</div><div class="v" style="font-size:18px">${sync.enabled?'actief':'optioneel'}</div></div></div><div class="quick-apps">${quick}</div><div class="grid two" style="margin-top:18px">${paideia}</div>`;$$('[data-app]').forEach(b=>b.onclick=()=>openApp(b.dataset.app))}
function renderApps(){const p=requireProfile();if(!p)return;const apps=allowedApps(p),st=S.getProfileData(p.id,'paideia_state',{streak:0,weekly_scores:[],monthly_scores:[],mastery:{}});const cards=apps.map(a=>{const m=appMeta(a);if(a==='scriptorium')return `<article class="app-detail"><div class="app-detail-head"><img src="${m.logo}"><div><h3>Scriptorium</h3><div class="tiny">Onderzoek & academische vaardigheden</div></div></div><div class="app-metrics"><div class="app-metric"><b>56</b><span>kernwerken</span></div><div class="app-metric"><b>56</b><span>geanalyseerd</span></div><div class="app-metric"><b>23</b><span>trainingsmodules</span></div></div><p class="muted">Beheer het corpus, zoek nieuwe werken en primaire bronnen, bestudeer theorie en train onderzoeksvaardigheden.</p><div class="row"><button class="btn primary" data-app="scriptorium">Open Scriptorium</button><span class="chip">toegang via dit profiel</span></div></article>`;const mastery=st.mastery||{},activePaths=Object.entries(st.learning_paths||{}).filter(([,v])=>v?.active).map(([k])=>k);const keys=activePaths.length?activePaths:Object.keys(mastery);const avg=keys.map(k=>mastery[k]).filter(x=>Number.isFinite(+x));const score=avg.length?Math.round(avg.reduce((x,y)=>x+(+y),0)/avg.length):0;return `<article class="app-detail"><div class="app-detail-head"><img src="${m.logo}"><div><h3>Paideia</h3><div class="tiny">Levenslang leren & kennisbehoud</div></div></div><div class="app-metrics"><div class="app-metric"><b>${st.streak||0}</b><span>streak</span></div><div class="app-metric"><b>${score}%</b><span>gem. mastery</span></div><div class="app-metric"><b>${(st.weekly_scores||[]).length}</b><span>weekexamens</span></div></div><p class="muted">Dagelijkse microcurricula, spaced repetition, kennisnetwerken, examencyclus en algemene kennis/skills.</p><div class="row"><button class="btn gold" data-app="paideia">Open Paideia</button><span class="chip">${st.last_completed?'recent gebruikt':'klaar om te starten'}</span></div></article>`}).join('');$('#page-apps').innerHTML=`<div class="hero"><div class="spread"><div><h2>Mijn apps</h2><p>Hier zie je alleen de toepassingen die aan ${esc(p.name)} zijn toegewezen. Home is de snelle ingang; deze pagina toont inhoud, voortgang en status per app.</p></div><button class="btn" id="manageAppAccess">Toegang beheren</button></div></div><div class="app-detail-grid">${cards||'<div class="no-apps">Nog geen apps toegewezen. Gebruik <strong>Toegang beheren</strong> om een app aan dit profiel te koppelen.</div>'}</div><div class="card" style="margin-top:18px"><h3>Profielen blijven gescheiden</h3><p class="muted">Een profiel krijgt alleen toegang tot zijn toegewezen modules. Latere farmacie- of vroegmoderne apps kunnen als nieuwe Athenaeum-modules worden toegevoegd zonder Scriptorium of Paideia automatisch voor iedereen zichtbaar te maken.</p></div>`;$('#manageAppAccess').onclick=()=>editProfile(p.id);$$('[data-app]').forEach(b=>b.onclick=()=>openApp(b.dataset.app))}
function renderPrimarySourceResults(){
  const p=requireProfile();if(!p)return;
  const q=($('#primarySourceSearch')?.value||'').trim().toLowerCase(),arr=S.getProfileData(p.id,'primary_source_meta',[]),box=$('#primarySourceResults');if(!box)return;
  const hits=!q?arr:arr.filter(x=>[x.title,x.author,x.canonical_ref,x.period,x.place,x.type,x.notes].join(' ').toLowerCase().includes(q));
  box.innerHTML=hits.length?hits.map(x=>`<div class="source-meta-row"><div><strong>${esc(x.title||'Zonder titel')}</strong><div class="tiny">${esc(x.author||'onbekend')} · ${esc(x.canonical_ref||x.type||'primaire bron')} ${x.period?'· '+esc(x.period):''}</div></div><button class="btn small" data-del-source="${esc(x.id)}">Verwijder</button></div>`).join(''):`<div class="source-empty"><strong>${arr.length?'Geen overeenkomst gevonden.':'Nog geen lokale primaire bronnen geregistreerd.'}</strong><br><span>Het zoekveld blijft bruikbaar. Voeg bronmetadata of een bestand toe, of open de bronzoeker in Scriptorium.</span></div>`;
  $$('[data-del-source]').forEach(b=>b.onclick=()=>{S.setProfileData(p.id,'primary_source_meta',arr.filter(x=>x.id!==b.dataset.delSource));renderPrimarySourceResults()});
}
function addPrimarySourceMeta(){const p=requireProfile();if(!p)return;const title=prompt('Titel / korte beschrijving van de primaire bron');if(!title)return;const author=prompt('Auteur / producent (indien bekend)')||'',canonical_ref=prompt('Canonieke referentie')||'',period=prompt('Periode / datering')||'',type=prompt('Bronsoort')||'';const arr=S.getProfileData(p.id,'primary_source_meta',[]);arr.push({id:S.uid(),title,author,canonical_ref,period,type,updated_at:Date.now()});S.setProfileData(p.id,'primary_source_meta',arr);renderPrimarySourceResults();AthSync.markDirty?.(p.id)}
function registerPrimaryFiles(files){const p=requireProfile();if(!p)return;const arr=S.getProfileData(p.id,'primary_source_meta',[]);for(const f of [...(files||[])])arr.push({id:S.uid(),title:f.name,type:f.type||'bestand',file_name:f.name,file_size:f.size,notes:'Bestandsmetadata lokaal geregistreerd; bestand zelf wordt niet naar de cloud geüpload.',updated_at:Date.now()});S.setProfileData(p.id,'primary_source_meta',arr);renderPrimarySourceResults();AthSync.markDirty?.(p.id);S.toast(`${files?.length||0} bestand(en) geregistreerd.`)}
function renderLibrary(){const p=requireProfile();if(!p)return;const lib=S.getProfileData(p.id,'library_meta',[]);$('#page-library').innerHTML=`<div class="hero"><h2>Bibliotheek</h2><p>Beheer je gecontroleerde werken, primaire bronmetadata en kennis-packs. Bestanden blijven lokaal; metadata kan met je profiel synchroniseren.</p></div><div class="card primary-explorer" style="margin-top:16px"><div class="spread"><div><h3>🏺 Primaire bronnenverkenner</h3><p class="muted">Zoek in geregistreerde primaire bronnen. Het zoekveld blijft actief, ook als je nog geen bestanden hebt toegevoegd.</p></div><span class="chip">profielgebonden</span></div><div class="source-search-row"><input id="primarySourceSearch" class="source-search-input" type="search" placeholder="Zoek in titel, auteur, referentie, periode of bronsoort…" autocomplete="off"><button class="btn" id="addPrimaryMeta">+ Bronmetadata</button><label class="btn">+ Bestand registreren<input id="primaryFileInput" type="file" multiple hidden></label>${hasApp('scriptorium',p)?'<button class="btn primary" id="openScriptoriumSources">Open Scriptorium-bronzoeker</button>':''}</div><div id="primarySourceResults" style="margin-top:12px"></div></div><div class="grid two" style="margin-top:16px"><div class="card"><h3>Voorrangsorde voor leerinhoud</h3><ol><li><strong>Jouw aangeleverde en gecontroleerde werken</strong></li><li>Universitaire compendia / handboeken</li><li>Wetenschappelijke monografieën en artikels</li><li>Modelkennis alleen als aanvulling</li></ol></div><div class="card"><h3>Kennispack importeren</h3><p>Na analyse van een boek of scan kan een JSON-pack met lessen, kennisnodes, vragen en exacte fysieke paginaverwijzingen worden geïmporteerd in Paideia.</p><label class="btn">Importeer pack<input id="knowledgePackFile" type="file" accept=".json" hidden></label><a class="btn" href="./content/knowledge_pack_schema.json" download>Schema downloaden</a></div></div><div class="card" style="margin-top:16px"><div class="spread"><h3>Geregistreerde werken</h3><button class="btn" id="addLibMeta">+ Metadata toevoegen</button></div><div id="libList" style="margin-top:10px">${lib.length?lib.map(x=>`<div style="padding:12px 0;border-bottom:1px solid #e2dacb"><strong>${esc(x.title)}</strong><div class="tiny">${esc(x.author||'')} · ${esc(x.classification||'')}</div></div>`).join(''):'<div class="muted">Nog geen werken geregistreerd.</div>'}</div></div>`;$('#knowledgePackFile').onchange=e=>importKnowledgePack(e.target.files[0]);$('#addLibMeta').onclick=()=>{const title=prompt('Titel');if(!title)return;const author=prompt('Auteur')||'';const classification=prompt('Categorie')||'';const arr=S.getProfileData(p.id,'library_meta',[]);arr.push({id:S.uid(),title,author,classification,updated_at:Date.now()});S.setProfileData(p.id,'library_meta',arr);renderLibrary()};$('#primarySourceSearch').oninput=renderPrimarySourceResults;$('#addPrimaryMeta').onclick=addPrimarySourceMeta;$('#primaryFileInput').onchange=e=>registerPrimaryFiles(e.target.files);if($('#openScriptoriumSources'))$('#openScriptoriumSources').onclick=()=>{const u=new URL('./apps/scriptorium/index.html',location.href);u.searchParams.set('ath_profile',p.id);u.searchParams.set('page','sources');location.href=u};renderPrimarySourceResults()}

async function importKnowledgePack(file){if(!file)return;let data;try{data=JSON.parse(await file.text())}catch{return S.toast('Ongeldig JSON-bestand.')}if(data.schema_version!==1||!Array.isArray(data.lessons))return S.toast('Geen geldig Athenaeum kennispack.');const p=current(),packs=S.getProfileData(p.id,'knowledge_packs',[]);const i=packs.findIndex(x=>x.pack_id===data.pack_id);if(i>=0)packs[i]=data;else packs.push(data);S.setProfileData(p.id,'knowledge_packs',packs);S.toast(`${data.lessons.length} lessen geïmporteerd.`)}
function renderProgress(){const p=requireProfile();if(!p)return;const st=S.getProfileData(p.id,'paideia_state',{streak:0,icecubes:2,weekly_scores:[],monthly_scores:[]}),ec=st.exam_config||{},w={enabled:true,day:0,weight:40,...(ec.weekly||{})},m={enabled:true,day_of_month:28,weight:60,...(ec.monthly||{})},threshold=Number(ec.freeze_threshold||75),formal=[...(st.weekly_scores||[]),...(st.monthly_scores||[])].filter(x=>x.grading_complete&&Number.isFinite(+x.score)),latest=formal.sort((a,b)=>(b.created_at||0)-(a.created_at||0))[0];$('#page-progress').innerHTML=`<div class="hero"><h2>Voortgang</h2><p>Athenaeum houdt leerdiscipline en kennisbeheersing uit elkaar. Alleen betrouwbaar beoordeelde resultaten worden als score behandeld.</p></div><div class="stat-grid"><div class="stat"><div class="k">Streak</div><div class="v">🔥 ${st.streak||0}</div></div><div class="stat"><div class="k">IJsblokjes</div><div class="v">🧊 ${st.icecubes??2}</div></div><div class="stat"><div class="k">Weekexamens</div><div class="v">${st.weekly_scores?.length||0}</div></div><div class="stat"><div class="k">Laatste betrouwbare score</div><div class="v">${latest?Math.round(latest.score)+'%':'—'}</div></div></div><div class="grid two" style="margin-top:18px"><div class="card"><h3>Regels van jouw profiel</h3><p>• Een gemiste geplande dag kan de streak breken, tenzij een ijsblokje wordt ingezet.<br>• Startvoorraad: 2 ijsblokjes.<br>• Een volledig beoordeeld examen met ≥${threshold}% kan 1 ijsblokje opleveren.<br>• Onbetrouwbaar of niet automatisch beoordeelbaar = geen fictieve score.</p></div><div class="card"><h3>Jouw evaluatieritme</h3><p>${w.enabled?`<strong>Weekexamen</strong>: ${['zondag','maandag','dinsdag','woensdag','donderdag','vrijdag','zaterdag'][Number(w.day)]} · ${w.weight}% · ${w.question_count||10} vragen.`:'<strong>Weekexamen</strong>: uit.'}<br>${m.enabled?`<strong>Mastery</strong>: dag ${m.day_of_month} · ${m.weight}% · ${m.question_count||14} vragen.`:'<strong>Mastery</strong>: uit.'}<br><strong>Dagelijks</strong>: theory-first retrieval en toepassing volgens je eigen leerpadplanning.</p></div></div>`}
function syncStatusHtml(p){const s=AthSync.status(p.id);if(!s.signed_in)return 'Niet aangemeld. Log in of herstel een cloudprofiel.';if(s.last_error)return `⚠️ Laatste fout: ${esc(s.last_error)}`;return `${s.email?`Aangemeld als ${esc(s.email)}. `:''}${s.last_sync?`Laatste sync: ${new Date(s.last_sync).toLocaleString('nl-BE')} (${esc(s.last_direction||'merge')}).`:'Nog geen succesvolle synchronisatie.'}`}
function safeDiagnosticReport(p){const d=detectDevice(),c=AthSync.cfg(p.id),st=S.getProfileData(p.id,'paideia_state',{}),store=typeof S.storageUsage==='function'?S.storageUsage():null;return{release:RELEASE,generated_at:new Date().toISOString(),runtime:{display_mode:isStandaloneApp()?'app':'browser',device:d.label,kind:d.kind,viewport:`${d.width}x${d.height}`,orientation:screen.orientation?.type||((innerWidth>innerHeight)?'landscape':'portrait'),touch_points:navigator.maxTouchPoints||0,online:navigator.onLine},profile:{apps:allowedApps(p),cloud_signed_in:!!c.user?.id,setup_complete:!!st.setup_complete,active_learning_paths:Object.entries(st.learning_paths||{}).filter(([,v])=>v?.active).map(([k])=>k),weekly_exam_enabled:st.exam_config?.weekly?.enabled!==false,monthly_exam_enabled:st.exam_config?.monthly?.enabled!==false},sync:{enabled:!!c.enabled,last_sync:c.last_sync||null,last_scriptorium_sync:c.last_scriptorium_sync||null,last_error:c.last_error||null,last_scriptorium_error:c.last_scriptorium_error||null},storage:store||'niet beschikbaar'}}
function diagnosticText(p){return JSON.stringify(safeDiagnosticReport(p),null,2)}
async function copyDiagnostic(p){try{await navigator.clipboard.writeText(diagnosticText(p));S.toast('Diagnoserapport gekopieerd zonder wachtwoord of tokens.')}catch{S.toast('Kopiëren niet toegestaan door de browser. Gebruik Download diagnose.')}}
function downloadDiagnostic(p){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([diagnosticText(p)],{type:'application/json'}));a.download=`Athenaeum_diagnose_${todayISO()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function renderSettings(){
  const p=requireProfile();if(!p)return;
  const c=AthSync.cfg(p.id),d=AthSync.defaults();
  $('#page-settings').innerHTML=`<div class="hero"><h2>Instellingen</h2><p>Beheer je profiel, synchronisatie en installatie. Paideia-meldingen en Telegram staan binnen Paideia → Instellingen.</p></div><div class="settings-section" style="margin-top:16px">
  <div class="card"><div class="spread"><div><h3>Account</h3><p class="muted">Naam, avatar, app-toegang en optionele PIN.</p></div><div class="row"><button class="btn" id="editAccount">Account aanpassen</button><button class="btn" id="openOnboarding">Installatiewizard</button></div></div></div>
  <div class="card"><div class="spread"><div><h3>Synchronisatie tussen apparaten</h3><p class="muted">Athenaeum gebruikt een lichte realtime-profielsync en synchroniseert Scriptorium incrementeel per werk. Daardoor kunnen meerdere apparaten tegelijk actief zijn zonder telkens alle analyses opnieuw in het geheugen te laden.</p></div><span class="chip">split sync v4</span></div>
    <div class="cloud-default-note">☁️ <strong>Athenaeum Cloud</strong><span>server vooraf ingesteld</span></div>
    <div class="grid two"><div class="field"><label>E-mail</label><input id="sbEmail" type="email" value="${esc(c.email||'')}" autocomplete="username"></div><div class="field"><label>Wachtwoord</label><input id="sbPass" type="password" autocomplete="current-password"></div></div>
    <details class="advanced-cloud"><summary>Geavanceerde serverinstellingen</summary><div class="grid two" style="margin-top:10px"><div class="field"><label>Supabase URL</label><input id="sbUrl" value="${esc(c.url||d.url)}"></div><div class="field"><label>Publishable key</label><input id="sbKey" value="${esc(c.key||d.key)}"></div></div></details>
    <div class="row" style="margin-top:12px"><button class="btn primary" id="signinBtn">Aanmelden & synchroniseren</button>${c.user?.id?'':'<button class="btn" id="signupBtn">Nieuw cloudaccount</button>'}<button class="btn" id="syncBtn">Alles synchroniseren</button><button class="btn" id="syncScriptBtn">Alleen Scriptorium</button><button class="btn" id="pullBtn">Cloud ophalen</button><button class="btn" id="pushBtn">Lokale versie uploaden</button><button class="btn" id="testSyncBtn">Test verbinding</button><button class="btn" id="signoutBtn">Afmelden</button></div>
    <div class="tiny" id="syncStatus" style="margin-top:10px">${syncStatusHtml(p)}</div><div class="tiny" id="scriptSyncStatus" style="margin-top:5px">Scriptorium: ${c.last_scriptorium_sync?`laatst incrementeel ${new Date(c.last_scriptorium_sync).toLocaleString()}`:"nog niet incrementeel gesynchroniseerd"}${c.last_scriptorium_error?` · ⚠️ ${esc(c.last_scriptorium_error)}`:""}</div><div class="diag-box" id="cloudDiag"><strong>Clouddiagnose</strong><div class="tiny">Controleert server, Auth en databank afzonderlijk.</div></div><div class="row" style="margin-top:8px"><button class="btn" id="diagCloudBtn">Diagnose cloud</button><a class="btn" href="${esc(c.url||d.url)}" target="_blank">Open serveradres</a></div><div class="tiny" style="margin-top:6px">Op een nieuwe computer kies je op het profielscherm <strong>Cloudprofiel herstellen</strong>. Athenaeum herkent hetzelfde Supabase-account en gebruikt vervolgens hetzelfde lokale profiel.</div>
  </div>
  <div class="card"><div class="spread"><div><h3>App installeren</h3><p class="muted" id="installDeviceInfo">Athenaeum controleert je apparaat…</p></div><span class="chip" id="installModeChip">PWA</span></div><div class="row" style="margin:12px 0"><button class="btn primary" id="installPwaBtn">Installeer Athenaeum</button><button class="btn" id="refreshDiag">Diagnose vernieuwen</button></div><div id="pwaDiag" class="grid two"></div><div class="tiny" style="margin-top:8px">Telefoon, tablet/iPad en laptop gebruiken dezelfde Athenaeum-installatie; de interface past zich automatisch aan het apparaat en de oriëntatie aan.</div></div>
  <div class="card"><div class="spread"><div><h3>Test & diagnose</h3><p class="muted">Maak een privacyvriendelijk rapport van apparaat, opslag, cloudstatus en actieve leerconfiguratie. Wachtwoorden, tokens en API-sleutels worden nooit opgenomen.</p></div><span class="chip">V1.2.1 test</span></div><div class="row" style="margin-top:12px"><button class="btn" id="copyDiagnostic">Kopieer diagnoserapport</button><button class="btn" id="downloadDiagnostic">Download diagnose</button></div><div class="tiny" style="margin-top:8px">Handig wanneer een vriend een fout meldt: stuur dit bestand mee zonder accountgeheimen te delen.</div></div>
  <div class="card"><h3>Export & levensduur</h3><p class="muted">Je profiel blijft ook zonder cloud bruikbaar.</p><div class="row"><button class="btn" id="exportProfile">Exporteer profiel JSON</button><label class="btn">Importeer profiel<input id="importProfileFile" type="file" accept=".json" hidden></label></div></div>
  </div>`;
  bindSettings();runPwaDiag();
}
function saveSyncForm(p){
  const c=AthSync.cfg(p.id),d=AthSync.defaults();
  AthSync.saveCfg(p.id,{...c,url:$('#sbUrl')?.value.trim()||d.url,key:$('#sbKey')?.value.trim()||d.key,email:$('#sbEmail').value.trim(),enabled:c.enabled});
  return AthSync.cfg(p.id);
}
async function refreshTelegramStatus(pid){const chip=$('#telegramStateChip'),box=$('#telegramPairStatus');if(!chip)return;try{const s=await AthTelegram.status(pid);chip.textContent=s.connected?'✅ gekoppeld':'niet gekoppeld';box.textContent=s.connected?`Telegram is gekoppeld${s.bot_username?' met @'+s.bot_username:''}.`:(s.error||'Maak een koppelcode en open daarna de bot in Telegram.')}catch(e){chip.textContent='controle nodig';box.textContent=e.message}}
function bindSettings(){
  const p=current();
  $('#editAccount').onclick=()=>editProfile(p.id);if($('#openOnboarding'))$('#openOnboarding').onclick=()=>startOnboarding(p.id,false);
  const signup=$('#signupBtn');
  if(signup)signup.onclick=async()=>{try{
    saveSyncForm(p);
    const d=await AthSync.auth(p.id,$('#sbEmail').value.trim(),$('#sbPass').value,true);
    if(d.access_token){await AthSync.syncAll(p.id);AthSync.startAuto(p.id);S.toast('Cloudaccount aangemaakt en gesynchroniseerd.')}
    else S.toast('Account aangemaakt. Bevestig eventueel je e-mail en meld daarna aan.');
    renderSettings();
  }catch(e){S.toast(e.message)}};
  $('#signinBtn').onclick=async()=>{try{
    saveSyncForm(p);
    await AthSync.auth(p.id,$('#sbEmail').value.trim(),$('#sbPass').value,false);
    await AthSync.syncAll(p.id);AthSync.startAuto(p.id);
    S.toast('Aangemeld en gesynchroniseerd.');renderSettings();
  }catch(e){S.toast(e.message)}};
  $('#syncBtn').onclick=async()=>{try{
    $('#syncStatus').textContent='Lichte data + Scriptorium synchroniseren…';
    await AthSync.syncAll(p.id);
    $('#syncStatus').textContent=syncStatusHtml(p);
    const sc=AthSync.cfg(p.id);
    $('#scriptSyncStatus').textContent=sc.last_scriptorium_sync?`Scriptorium: laatst incrementeel ${new Date(sc.last_scriptorium_sync).toLocaleString()}`:'Scriptorium: nog niet gesynchroniseerd';
    S.toast('Alles gesynchroniseerd zonder volledige corpusdump.');
  }catch(e){$('#syncStatus').textContent='⚠️ '+e.message}};
  $('#syncScriptBtn').onclick=async()=>{try{
    $('#scriptSyncStatus').textContent='Scriptorium incrementeel synchroniseren…';
    const r=await AthSync.syncScriptorium(p.id);
    $('#scriptSyncStatus').textContent=`Scriptorium bijgewerkt · ↓${r.downloaded||0} ↑${r.uploaded||0} · instellingen ↓${r.settings_down||0} ↑${r.settings_up||0}`;
    S.toast('Scriptorium incrementeel gesynchroniseerd.');
  }catch(e){$('#scriptSyncStatus').textContent='⚠️ '+e.message}};
  $('#pullBtn').onclick=async()=>{try{$('#syncStatus').textContent='Cloud ophalen…';await AthSync.pullOnly(p.id);$('#syncStatus').textContent=syncStatusHtml(p);S.toast('Profiel/Paideia cloudgegevens lokaal gemerged.')}catch(e){$('#syncStatus').textContent='⚠️ '+e.message}};
  $('#pushBtn').onclick=async()=>{if(!confirm('Lokale gegevens nu als cloudbasis uploaden? Gebruik normaal “Nu synchroniseren”.'))return;try{await AthSync.pushOnly(p.id);$('#syncStatus').textContent=syncStatusHtml(p);S.toast('Profiel/Paideia lokaal naar cloud geüpload.')}catch(e){$('#syncStatus').textContent='⚠️ '+e.message}};
  $('#testSyncBtn').onclick=async()=>{try{const r=await AthSync.testConnection(p.id);S.toast(r.server?'Cloudserver bereikbaar.':'Cloudcontrole mislukt.')}catch(e){S.toast(e.message)}};
  $('#diagCloudBtn').onclick=async()=>{const box=$('#cloudDiag');box.innerHTML='<strong>Clouddiagnose</strong><div class="tiny">Controleren…</div>';const r=await AthSync.diagnoseConnection(p.id);box.innerHTML=`<strong>${r.server&&r.scriptorium_tables?'✅ Cloud + split sync bereikbaar':r.server?'⚠️ Cloud bereikbaar, Scriptorium-sync controleren':'⚠️ Cloudprobleem'}</strong><div class="diag-steps">${(r.details||[]).map(x=>`<div>${esc(x)}</div>`).join('')}</div>${r.error?`<div class="tiny warn-text">${esc(r.error)}</div>`:''}`};
  $('#signoutBtn').onclick=async()=>{await AthSync.signOut(p.id);renderSettings();S.toast('Cloudsessie afgemeld.')};
  $('#copyDiagnostic')?.addEventListener('click',()=>copyDiagnostic(p));$('#downloadDiagnostic')?.addEventListener('click',()=>downloadDiagnostic(p));$('#installPwaBtn').onclick=requestPwaInstall;$('#refreshDiag').onclick=()=>{runPwaDiag();updateMode()};refreshInstallUi();$('#exportProfile').onclick=()=>exportProfile(p.id);$('#importProfileFile').onchange=e=>importProfile(e.target.files[0]);
}
function findExistingCloudProfile(r,rp={}){
  const ps=S.loadProfiles(),uid=r.auth?.user?.id,email=(r.auth?.user?.email||r.email||'').toLowerCase();
  return ps.find(p=>p.id===rp.id)
      || ps.find(p=>p.cloud_user_id===uid)
      || ps.find(p=>AthSync.cfg(p.id).user?.id===uid)
      || ps.find(p=>(p.cloud_email||AthSync.cfg(p.id).email||'').toLowerCase()===email);
}
async function restoreCloudProfile(){
  const status=$('#restoreCloudStatus'),d=AthSync.defaults();
  status.textContent='Aanmelden en cloudprofiel zoeken…';
  try{
    const r=await AthSync.restoreRemote({url:$('#restoreSbUrl')?.value.trim()||d.url,key:$('#restoreSbKey')?.value.trim()||d.key,email:$('#restoreEmail').value.trim(),password:$('#restorePass').value});
    const rp=r.payload.profile||{};
    let p=findExistingCloudProfile(r,rp);
    if(!p)p=await S.createProfile({name:rp.name||r.email||'Cloudprofiel',avatar:rp.avatar||'bust',apps:Array.isArray(rp.apps)?rp.apps:[]});
    const ps=S.loadProfiles(),i=ps.findIndex(x=>x.id===p.id);
    if(i>=0){
      ps[i]={...ps[i],name:rp.name||ps[i].name,avatar:rp.avatar||ps[i].avatar,apps:Array.isArray(rp.apps)?rp.apps:ps[i].apps,icecubes:rp.icecubes??ps[i].icecubes,pin_hash:rp.pin_hash??ps[i].pin_hash,pin_salt:rp.pin_salt||ps[i].pin_salt,cloud_user_id:r.auth.user.id,cloud_email:(r.auth.user.email||r.email||'').toLowerCase(),updated_at:Math.max(ps[i].updated_at||0,rp.updated_at||0,Date.now())};
      S.saveProfiles(ps);p=ps[i];
    }
    AthSync.adoptSession(p.id,r);
    await AthSync.apply(p.id,{...r.payload,profile:{...rp,id:p.id,cloud_user_id:r.auth.user.id,cloud_email:(r.auth.user.email||r.email||'').toLowerCase()}});
    status.textContent='Profiel hersteld. Scriptorium wordt per werk binnengehaald…';
    await AthSync.syncScriptorium(p.id,{preferRemote:true});
    closeModal('cloudRestoreModal');renderProfiles();
    S.toast('Cloudprofiel en Scriptorium hersteld zonder zware totaaldownload.');
  }catch(e){status.textContent='⚠️ '+e.message}
}
function runPwaDiag(){const box=$('#pwaDiag');if(!box)return;const standalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;const checks=[['HTTPS',location.protocol==='https:'||location.hostname==='localhost'],['Manifest',!!document.querySelector('link[rel="manifest"]')],['Service worker',!!navigator.serviceWorker],['Standalone',standalone]];box.innerHTML=checks.map(([k,v])=>`<div class="card" style="padding:12px;box-shadow:none"><strong>${v?'✅':'⚠️'} ${k}</strong><div class="tiny">${v?'OK':'controle nodig'}</div></div>`).join('')}
function exportProfile(id){const p=S.loadProfiles().find(x=>x.id===id),data={athenaeum_export:1,athenaeum_release:RELEASE,exported_at:new Date().toISOString(),profile:{...p,pin_hash:'',pin_salt:''},data:{}};Object.keys(localStorage).filter(k=>k.startsWith(`ath_${id}_`)&&!k.endsWith('_sync')).forEach(k=>{try{data.data[k.slice(`ath_${id}_`.length)]=JSON.parse(localStorage.getItem(k))}catch{}});const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download=`Athenaeum_${p.name.replace(/\W+/g,'_')}_${todayISO()}.json`;a.click();URL.revokeObjectURL(a.href)}
async function importProfile(file){if(!file)return;let d;try{d=JSON.parse(await file.text())}catch{return S.toast('Ongeldige JSON.')}if(d.athenaeum_export!==1)return S.toast('Geen Athenaeum-profielexport.');const p=await S.createProfile({name:d.profile?.name||'Geïmporteerd',avatar:d.profile?.avatar||'bust',apps:Array.isArray(d.profile?.apps)?d.profile.apps:[],pin:''});if(Number.isFinite(+d.profile?.icecubes)){const ps=S.loadProfiles(),i=ps.findIndex(x=>x.id===p.id);if(i>=0){ps[i].icecubes=+d.profile.icecubes;S.saveProfiles(ps)}}for(const [k,v] of Object.entries(d.data||{}))S.setProfileData(p.id,k,v);S.toast('Profiel geïmporteerd.');renderProfiles()}

const ONBOARD_PATHS=[
  ['ancient','🏺','Oude Geschiedenis',20,'high',false],['early_modern','🕯️','Vroegmoderne Tijd',20,'high',false],['general_history','🌍','Algemene Geschiedenis',10,'normal',true],['general_knowledge','🧠','Algemene Kennis',10,'light',true],['pharmacy','⚗️','Farmacie · work in progress',20,'normal',false],['skill','🛠️','Algemene Vaardigheden',12,'light',false]
];
const ONBOARD_DAYS=[['1','maandag'],['2','dinsdag'],['3','woensdag'],['4','donderdag'],['5','vrijdag'],['6','zaterdag'],['0','zondag']];
function wizardDayPreset(days){const a=(days||[]).map(Number).sort().join(','),map={'1,2,3,4,5':'weekdays','0,1,2,3,4,5,6':'daily','0,6':'weekend'};return map[a]||'manual'}
function wizardPresetDays(v,existing=[]){if(v==='weekdays')return[1,2,3,4,5];if(v==='daily')return[0,1,2,3,4,5,6];if(v==='weekend')return[6,0];return existing?.length?existing:[1,2,3,4,5]}
function normalizeWizardWeights(we,ww,me,mw){if(we&&!me)return[100,0];if(!we&&me)return[0,100];if(!we&&!me)return[0,0];const sum=Math.max(1,Number(ww||0)+Number(mw||0));return[Math.round(Number(ww||0)*100/sum),100-Math.round(Number(ww||0)*100/sum)]}
function paideiaDefaultsForWizard(pid){const old=S.getProfileData(pid,'paideia_state',{}),base={version:5,streak:0,icecubes:2,last_completed:'',completed_dates:[],daily_count:0,ancient_index:0,early_modern_index:0,general_index:0,gk_index:0,pharmacy_index:0,skill_index:0,weekly_scores:[],monthly_scores:[],history:[],generated_questions:[],mastery:{ancient:0,early_modern:0,general_history:0,general_knowledge:0,pharmacy:0,skill:0}};const lp={};for(const[k,, ,minutes,priority,active]of ONBOARD_PATHS){const historical=['ancient','early_modern','general_history'].includes(k);lp[k]={active,priority,minutes,exam:historical,telegram:true,study_days:[1,2,3,4,5,6,0],...(old.learning_paths?.[k]||{})}}return{...base,...old,plan_config:{max_daily_minutes:60,light_every:3,weekly_notice_days:2,monthly_notice_days:3,...(old.plan_config||{})},learning_paths:{...lp,...(old.learning_paths||{})},exam_config:{weekly:{enabled:true,day:0,weight:40,duration_min:45,question_count:10,...(old.exam_config?.weekly||{})},monthly:{enabled:true,day_of_month:28,weight:60,duration_min:75,question_count:14,...(old.exam_config?.monthly||{})},freeze_threshold:old.exam_config?.freeze_threshold||75},updated_at:Date.now()}}
function startOnboarding(pid,automatic=false){const p=S.loadProfiles().find(x=>x.id===pid);if(!p)return;if(automatic&&S.getProfileData(pid,'athenaeum_onboarding',{})?.done)return;onboardingPid=pid;onboardingStep=0;$('#onboardingModal')?.classList.add('open');renderOnboarding()}
function onboardingFinish(){if(!onboardingPid)return;const st=paideiaDefaultsForWizard(onboardingPid);st.setup_complete=true;st.updated_at=Date.now();S.setProfileData(onboardingPid,'paideia_state',st);S.setProfileData(onboardingPid,'athenaeum_onboarding',{done:true,completed_at:Date.now()});$('#onboardingModal')?.classList.remove('open');S.toast('Persoonlijke Athenaeum-instelling voltooid.');renderShell();showPage('home')}
function wizardNav(back=true,next=true){return`<div class="row wizard-actions" style="margin-top:18px">${back?'<button class="btn" id="wizardBack">Vorige</button>':''}${next?'<button class="btn primary" id="wizardNext">Volgende</button>':''}</div>`}
function bindWizardNav(){if($('#wizardBack'))$('#wizardBack').onclick=()=>{onboardingStep=Math.max(0,onboardingStep-1);renderOnboarding()};if($('#wizardNext'))$('#wizardNext').onclick=()=>{onboardingStep=Math.min(4,onboardingStep+1);renderOnboarding()}}
function renderOnboarding(){const p=S.loadProfiles().find(x=>x.id===onboardingPid);if(!p)return;const host=$('#onboardingHost'),st=paideiaDefaultsForWizard(onboardingPid),apps=S.profileApps(p),titles=['Welkom','Leerpaden','Examens','Cloud','Telegram'];$('#onboardingTitle').textContent=titles[onboardingStep];$('#onboardingStepChip').textContent=`Stap ${onboardingStep+1} van 5`;$('#onboardingBar').style.width=`${(onboardingStep+1)*20}%`;if(onboardingStep===0){host.innerHTML=`<div class="wizard-panel"><h3>Welkom, ${esc(p.name)}</h3><p>Dit profiel heeft zijn <strong>eigen apps, leerpaden, planning, examens, Telegramkoppeling en voortgang</strong>. Wat jij hier instelt verandert niets voor andere gebruikers.</p><div class="wizard-apps">${apps.map(a=>{const m=appMeta(a);return`<div><img src="${m.logo}"><strong>${m.name}</strong><span>${m.desc}</span></div>`}).join('')||'<p>Er zijn nog geen apps gekozen. Pas eerst je account aan.</p>'}</div><p class="tiny">Je kunt alles later opnieuw wijzigen via Athenaeum/Paideia → Instellingen.</p>${wizardNav(false,true)}</div>`;bindWizardNav();return}
if(onboardingStep===1){if(!apps.includes('paideia')){host.innerHTML=`<div class="wizard-panel"><h3>Paideia is niet aan dit profiel toegewezen</h3><p>Je kunt deze stap overslaan of eerst via Account Paideia toevoegen.</p>${wizardNav(true,true)}</div>`;bindWizardNav();return}host.innerHTML=`<div class="wizard-panel"><h3>Kies je leerpaden en ritme</h3><p>Elk profiel heeft zijn eigen curriculum. Kies alleen wat je werkelijk wilt volgen; je kunt later per leerpad de exacte weekdagen verder verfijnen.</p><div class="wizard-path-list">${ONBOARD_PATHS.map(([k,ico,label,minutes])=>{const x=st.learning_paths[k],preset=wizardDayPreset(x.study_days);return`<div class="wizard-path"><label><input type="checkbox" data-w-path="${k}" ${x.active?'checked':''}><span><strong>${ico} ${esc(label)}</strong><small>${k==='pharmacy'?'Nog in ontwikkeling; alleen activeren met eigen gecontroleerde packs.':'Theorie → retrieval → toepassing.'}</small></span></label><select data-w-priority="${k}" title="Prioriteit"><option value="high" ${x.priority==='high'?'selected':''}>hoog</option><option value="normal" ${x.priority==='normal'?'selected':''}>normaal</option><option value="light" ${x.priority==='light'?'selected':''}>licht</option></select><input data-w-minutes="${k}" type="number" min="5" max="90" step="5" value="${x.minutes||minutes}" title="minuten per sessie"><select data-w-days="${k}" title="Trainingsdagen"><option value="weekdays" ${preset==='weekdays'?'selected':''}>ma–vr</option><option value="daily" ${preset==='daily'?'selected':''}>dagelijks</option><option value="weekend" ${preset==='weekend'?'selected':''}>weekend</option><option value="manual" ${preset==='manual'?'selected':''}>bestaande eigen dagen</option></select></div>`}).join('')}</div><div class="wizard-box"><div class="field"><label>Maximum automatische studieduur per dag</label><input id="wizardMaxDaily" type="number" min="10" max="180" step="5" value="${Number(st.plan_config?.max_daily_minutes||60)}"></div><p class="tiny">Paideia overschrijdt deze limiet niet bij de automatische dagsessie; je kunt altijd handmatig extra oefenen.</p></div>${wizardNav(true,false)}<button class="btn primary" id="wizardPathsSave">Bewaar & volgende</button></div>`;$('#wizardBack').onclick=()=>{onboardingStep--;renderOnboarding()};$('#wizardPathsSave').onclick=()=>{let active=0;for(const[k]of ONBOARD_PATHS){const was=st.learning_paths[k]||{},on=!!$(`[data-w-path="${k}"]`)?.checked;if(on)active++;st.learning_paths[k]={...was,active:on,priority:$(`[data-w-priority="${k}"]`)?.value||'normal',minutes:Math.max(5,+$(`[data-w-minutes="${k}"]`)?.value||20),study_days:wizardPresetDays($(`[data-w-days="${k}"]`)?.value,was.study_days)}}if(!active)return S.toast('Kies minstens één leerpad voor Paideia.');st.plan_config={...(st.plan_config||{}),max_daily_minutes:Math.max(10,Math.min(180,+$('#wizardMaxDaily').value||60))};st.updated_at=Date.now();S.setProfileData(onboardingPid,'paideia_state',st);onboardingStep++;renderOnboarding()};return}
if(onboardingStep===2){const w=st.exam_config.weekly,m=st.exam_config.monthly,activePaths=ONBOARD_PATHS.filter(([k])=>st.learning_paths[k]?.active);host.innerHTML=`<div class="wizard-panel"><h3>Plan je examens</h3><p>Dit schema en deze weging horen uitsluitend bij dit profiel. AI-oefenvragen buiten een formeel examen tellen nooit stilzwijgend mee.</p><div class="grid two"><div class="wizard-box"><label class="mini-check"><input id="wWeeklyEnabled" type="checkbox" ${w.enabled?'checked':''}> Weekexamen</label><div class="field"><label>Dag</label><select id="wWeeklyDay">${ONBOARD_DAYS.map(([v,l])=>`<option value="${v}" ${Number(w.day)===Number(v)?'selected':''}>${l}</option>`).join('')}</select></div><div class="grid two"><div class="field"><label>Duur (min)</label><input id="wWeeklyDuration" type="number" min="10" max="180" value="${w.duration_min||45}"></div><div class="field"><label>Vragen</label><input id="wWeeklyCount" type="number" min="3" max="40" value="${w.question_count||10}"></div></div><div class="field"><label>Weging (%)</label><input id="wWeeklyWeight" type="number" min="0" max="100" value="${w.weight}"></div></div><div class="wizard-box"><label class="mini-check"><input id="wMonthlyEnabled" type="checkbox" ${m.enabled?'checked':''}> Maandelijkse mastery</label><div class="field"><label>Dag van de maand (1–28)</label><input id="wMonthlyDay" type="number" min="1" max="28" value="${m.day_of_month}"></div><div class="grid two"><div class="field"><label>Duur (min)</label><input id="wMonthlyDuration" type="number" min="15" max="300" value="${m.duration_min||75}"></div><div class="field"><label>Vragen</label><input id="wMonthlyCount" type="number" min="4" max="60" value="${m.question_count||14}"></div></div><div class="field"><label>Weging (%)</label><input id="wMonthlyWeight" type="number" min="0" max="100" value="${m.weight}"></div></div></div><div class="wizard-box"><strong>Welke leerpaden mogen in examens komen?</strong><div class="row" style="margin-top:8px">${activePaths.map(([k,ico,label])=>`<label class="mini-check"><input type="checkbox" data-w-exam-path="${k}" ${st.learning_paths[k]?.exam?'checked':''}> ${ico} ${esc(label)}</label>`).join('')||'<span class="tiny">Nog geen actieve leerpaden.</span>'}</div><p class="tiny">Je kunt Algemene Kennis en Algemene Vaardigheden dus opnemen, of examens beperken tot universitaire geschiedenis.</p></div>${wizardNav(true,false)}<button class="btn primary" id="wizardExamSave">Bewaar & volgende</button></div>`;$('#wizardBack').onclick=()=>{onboardingStep--;renderOnboarding()};$('#wizardExamSave').onclick=()=>{const we=$('#wWeeklyEnabled').checked,me=$('#wMonthlyEnabled').checked,[ww,mw]=normalizeWizardWeights(we,+$('#wWeeklyWeight').value,me,+$('#wMonthlyWeight').value);st.exam_config.weekly={...w,enabled:we,day:+$('#wWeeklyDay').value,weight:ww,duration_min:Math.max(10,+$('#wWeeklyDuration').value||45),question_count:Math.max(3,+$('#wWeeklyCount').value||10)};st.exam_config.monthly={...m,enabled:me,day_of_month:Math.max(1,Math.min(28,+$('#wMonthlyDay').value||28)),weight:mw,duration_min:Math.max(15,+$('#wMonthlyDuration').value||75),question_count:Math.max(4,+$('#wMonthlyCount').value||14)};for(const[k]of activePaths)st.learning_paths[k]={...st.learning_paths[k],exam:!!$(`[data-w-exam-path="${k}"]`)?.checked};if((we||me)&&!activePaths.some(([k])=>st.learning_paths[k]?.exam))return S.toast('Kies minstens één leerpad voor je examens.');st.updated_at=Date.now();S.setProfileData(onboardingPid,'paideia_state',st);onboardingStep++;renderOnboarding()};return}
if(onboardingStep===3){const c=AthSync.cfg(onboardingPid);host.innerHTML=`<div class="wizard-panel"><h3>Cloudaccount voor eigen apparaten en Telegram</h3><p>Iedere gebruiker gebruikt <strong>zijn eigen e-mailadres en wachtwoord</strong>. Deel nooit één cloudaccount met meerdere personen.</p><div class="grid two"><div class="field"><label>E-mail</label><input id="wizardEmail" type="email" value="${esc(c.email||'')}"></div><div class="field"><label>Wachtwoord</label><input id="wizardPass" type="password"></div></div><div id="wizardCloudStatus" class="tiny" style="margin-top:8px">${c.user?.id?'✅ Dit profiel is al aangemeld.':'Cloud is optioneel voor lokaal gebruik, maar nodig voor persoonlijke Telegramkoppeling en sync.'}</div><div class="row" style="margin-top:12px"><button class="btn primary" id="wizardCloudCreate">${c.user?.id?'Opnieuw synchroniseren':'Cloudaccount maken'}</button><button class="btn" id="wizardCloudSkip">Later</button><button class="btn" id="wizardBack">Vorige</button></div></div>`;$('#wizardBack').onclick=()=>{onboardingStep--;renderOnboarding()};$('#wizardCloudSkip').onclick=()=>{onboardingStep++;renderOnboarding()};$('#wizardCloudCreate').onclick=async()=>{const status=$('#wizardCloudStatus');try{if(c.user?.id){await AthSync.syncAll(onboardingPid);status.textContent='✅ Gesynchroniseerd.';onboardingStep++;setTimeout(renderOnboarding,350);return}const email=$('#wizardEmail').value.trim(),pass=$('#wizardPass').value;if(!email||pass.length<6)return S.toast('Vul een e-mailadres en wachtwoord van minstens 6 tekens in.');AthSync.saveCfg(onboardingPid,{...c,email,enabled:c.enabled});const d=await AthSync.auth(onboardingPid,email,pass,true);if(d.access_token){await AthSync.syncAll(onboardingPid);AthSync.startAuto(onboardingPid);status.textContent='✅ Cloudaccount aangemaakt en gesynchroniseerd.'}else status.textContent='Account aangemaakt. Bevestig eventueel eerst de e-mail en meld daarna aan.';onboardingStep++;setTimeout(renderOnboarding,500)}catch(e){status.textContent='⚠️ '+e.message}};return}
const notify=window.AthTelegram?.notify(onboardingPid)||{morning:'08:00',reminder:'18:00',timezone:'Europe/Brussels'},signed=!!AthSync.cfg(onboardingPid)?.user?.id;host.innerHTML=`<div class="wizard-panel"><h3>Telegram koppelen</h3><p>Alle gebruikers gebruiken dezelfde Athenaeum Bot, maar iedere chat wordt aan het eigen cloudaccount en profiel gekoppeld. Daardoor krijgt iedereen zijn eigen planning.</p><div class="grid three"><div class="field"><label>Ochtend</label><input id="wizardMorning" type="time" value="${esc(notify.morning||'08:00')}"></div><div class="field"><label>Reminder</label><input id="wizardReminder" type="time" value="${esc(notify.reminder||'18:00')}"></div><div class="field"><label>Tijdzone</label><input id="wizardTimezone" value="${esc(notify.timezone||'Europe/Brussels')}"></div></div><div id="wizardTelegramStatus" class="callout" style="margin-top:12px">${signed?'Je kunt nu een persoonlijke koppelcode maken.':'Meld eerst aan met een eigen cloudaccount; je kunt Telegram later koppelen in Paideia → Instellingen.'}</div><div class="row" style="margin-top:12px"><button class="btn primary" id="wizardPair" ${signed?'':'disabled'}>Koppel Telegram</button><button class="btn gold" id="wizardFinish">Voltooien</button><button class="btn" id="wizardBack">Vorige</button></div></div>`;$('#wizardBack').onclick=()=>{onboardingStep--;renderOnboarding()};$('#wizardPair').onclick=async()=>{const n={morning:$('#wizardMorning').value,reminder:$('#wizardReminder').value,timezone:$('#wizardTimezone').value};AthTelegram.saveNotify(onboardingPid,n);try{await AthTelegram.updateSchedule(onboardingPid,n);const r=await AthTelegram.pair(onboardingPid);$('#wizardTelegramStatus').innerHTML=`Koppelcode <strong>${esc(r.code)}</strong> is 30 minuten geldig. ${r.link?`<a href="${r.link}" target="_blank">Open @${esc(r.bot_username)} in Telegram</a>`:`Stuur /start ${esc(r.code)} naar de Athenaeum Bot.`}`}catch(e){$('#wizardTelegramStatus').textContent='⚠️ '+e.message}};$('#wizardFinish').onclick=()=>{const n={morning:$('#wizardMorning').value,reminder:$('#wizardReminder').value,timezone:$('#wizardTimezone').value};window.AthTelegram?.saveNotify(onboardingPid,n);onboardingFinish()}}

function showPage(name){
  const p=requireProfile();if(!p)return;
  const page=$('#page-'+name);if(!page)return;
  $$('.page').forEach(x=>x.classList.add('hidden'));
  page.classList.remove('hidden');
  $$('[data-page]').forEach(x=>x.classList.toggle('active',x.dataset.page===name));
  const titles={home:['Home','Snel verder vanuit je persoonlijke leer- en onderzoeksomgeving'],apps:['Mijn apps','Inhoud, toegang en voortgang per toegewezen toepassing'],library:['Bibliotheek','Bronnen en kennis-packs'],progress:['Voortgang','Streaks, examens en mastery'],settings:['Instellingen','Account, app-toegang, sync, meldingen en installatie']};
  $('#pageTitle').textContent=titles[name][0];
  $('#pageSubtitle').textContent=titles[name][1];
  ({home:renderHome,apps:renderApps,library:renderLibrary,progress:renderProgress,settings:renderSettings}[name])();
  scrollTo({top:0,behavior:'smooth'})
}
let deferredInstallPrompt=null;

function isStandaloneApp(){
  return window.matchMedia?.('(display-mode: standalone)').matches
      || window.matchMedia?.('(display-mode: fullscreen)').matches
      || window.matchMedia?.('(display-mode: minimal-ui)').matches
      || navigator.standalone===true
      || document.referrer.startsWith('android-app://');
}

function detectDevice(){
  const forced=(new URLSearchParams(location.search).get('view')||localStorage.getItem('athenaeum_view_mode')||'auto');
  if(forced==='mobile')return{kind:'phone',label:'telefoon',width:innerWidth,height:innerHeight};
  if(forced==='tablet')return{kind:'tablet',label:'tablet',width:innerWidth,height:innerHeight};
  if(forced==='desktop')return{kind:'desktop',label:'laptop',width:innerWidth,height:innerHeight};
  const ua=navigator.userAgent||'';
  const touch=(navigator.maxTouchPoints||0)>0;
  const ipad=/iPad/i.test(ua)||(navigator.platform==='MacIntel'&&touch);
  const vv=Math.round(window.visualViewport?.width||window.innerWidth||document.documentElement.clientWidth||1024);
  const vh=Math.round(window.visualViewport?.height||window.innerHeight||document.documentElement.clientHeight||768);
  const shortSide=Math.min(vv,vh);
  const longSide=Math.max(vv,vh);

  if(ipad) return {kind:'tablet',label:'iPad',width:vv,height:vh};
  if(touch && shortSide>=600 && longSide<=1500) return {kind:'tablet',label:'tablet',width:vv,height:vh};
  if(vv<=720) return {kind:'phone',label:'telefoon',width:vv,height:vh};
  if(touch && vv<=1180) return {kind:'tablet',label:'tablet',width:vv,height:vh};
  return {kind:'desktop',label:'laptop',width:vv,height:vh};
}

function updateMode(){
  const d=detectDevice();
  const standalone=isStandaloneApp();
  document.body.classList.remove('device-phone','device-tablet','device-desktop');
  document.body.classList.add('device-'+d.kind);
  document.documentElement.dataset.device=d.kind;
  document.documentElement.dataset.displayMode=standalone?'app':'web';
  const chip=$('#modeChip');
  if(chip) chip.textContent=`${standalone?'App':'Browser'} · ${d.label} · ${d.width}×${d.height} · ${navigator.onLine?'online':'offline'}`;
  refreshInstallUi?.();
}

window.addEventListener('online',updateMode);window.addEventListener('offline',updateMode);
function installHelpText(){
  const d=detectDevice(), standalone=isStandaloneApp(), ua=navigator.userAgent||'';
  const ios=/iPhone|iPad|iPod/i.test(ua)||(navigator.platform==='MacIntel'&&(navigator.maxTouchPoints||0)>1);
  if(standalone) return `✅ Athenaeum draait als geïnstalleerde app op deze ${d.label}.`;
  if(ios) return `Op iPhone/iPad: open Athenaeum in Safari → Deel-knop → ‘Zet op beginscherm’. Daarna opent Athenaeum zonder browserbalk als app.`;
  if(d.kind==='phone'||d.kind==='tablet') return `Op Android: open Athenaeum in Chrome → ⋮ → ‘App installeren’. Kies niet alleen ‘Snelkoppeling toevoegen’ als Chrome de echte app-optie aanbiedt.`;
  return `Op laptop/desktop: gebruik de installatieknop in de adresbalk of de knop hieronder zodra je browser die aanbiedt.`;
}

async function requestPwaInstall(){
  if(isStandaloneApp()){S.toast('Athenaeum is al als app geopend.');return}
  if(!deferredInstallPrompt){S.toast(installHelpText());return}
  deferredInstallPrompt.prompt();
  try{await deferredInstallPrompt.userChoice}catch{}
  deferredInstallPrompt=null;
  updateMode();
}

function refreshInstallUi(){
  const info=$('#installDeviceInfo'),btn=$('#installPwaBtn');
  if(info) info.textContent=installHelpText();
  if(btn){
    const standalone=isStandaloneApp();
    btn.textContent=standalone?'✓ App geïnstalleerd':'Installeer Athenaeum';
    btn.disabled=standalone;
  }
}

function init(){
  updateMode();
  S.ensureStarterProfiles();
  const cloudDefaults=AthSync.defaults();
  if($('#restoreSbUrl'))$('#restoreSbUrl').value=cloudDefaults.url;
  if($('#restoreSbKey'))$('#restoreSbKey').value=cloudDefaults.key;
  S.setCurrentProfile('');renderProfiles();const qp=new URLSearchParams(location.search);pendingLaunch=['paideia','scriptorium'].includes(qp.get('open'))?qp.get('open'):null;pendingPage=['home','apps','library','progress','settings'].includes(qp.get('page'))?qp.get('page'):'home';const requestedProfile=qp.get('profile'),resume=qp.get('resume')==='1';if(resume&&requestedProfile&&S.loadProfiles().some(p=>p.id===requestedProfile)){
  const p=S.loadProfiles().find(x=>x.id===requestedProfile);
  if(p?.pin_hash&&!sessionStorage.getItem('athenaeum_unlocked_'+requestedProfile)){setProfileLocked(true);pendingUnlock=requestedProfile;openModal('pinModal')}
  else enterProfile(requestedProfile)
}else{clearProtectedView();setProfileLocked(true)}$$('[data-close]').forEach(b=>b.onclick=()=>closeModal(b.dataset.close));$('#saveProfileBtn').onclick=saveProfile;$('#removePinBtn').onclick=async()=>{if(editing&&confirm('PIN van dit profiel verwijderen?')){const ps=S.loadProfiles(),i=ps.findIndex(p=>p.id===editing);if(i>=0){ps[i].pin_hash='';ps[i].updated_at=Date.now();S.saveProfiles(ps);closeModal('profileModal');renderShell();S.toast('PIN verwijderd.')}}};$('#deleteProfileBtn').onclick=()=>{if(editing&&confirm('Dit profiel lokaal verwijderen?')){S.removeProfile(editing);closeModal('profileModal');leaveProfile()}};$('#unlockBtn').onclick=unlock;$('#unlockPin').addEventListener('keydown',e=>{if(e.key==='Enter')unlock()});$$('#mainNav [data-page],#mobileNav [data-page]').forEach(b=>b.onclick=()=>showPage(b.dataset.page));$('#switchProfile').onclick=leaveProfile;$('#parentProfileBack')?.addEventListener('click',leaveProfile);$('#parentMobileBack')?.addEventListener('click',leaveProfile);$('#mobileMore').onclick=()=>$('#moreSheet').classList.add('open');$('#closeSheet').onclick=()=>$('#moreSheet').classList.remove('open');$('#sheetSwitch').onclick=leaveProfile;$('#sheetScriptorium').onclick=()=>openApp('scriptorium');$('#sheetPaideia').onclick=()=>openApp('paideia');$$('#moreSheet [data-page]').forEach(b=>b.onclick=()=>{$('#moreSheet').classList.remove('open');showPage(b.dataset.page)});$('#restoreCloudBtn')?.addEventListener('click',()=>openModal('cloudRestoreModal'));$('#restoreCloudSubmit')?.addEventListener('click',restoreCloudProfile);$('#onboardingLater')?.addEventListener('click',()=>{$('#onboardingModal')?.classList.remove('open');if(onboardingPid)S.setProfileData(onboardingPid,'athenaeum_onboarding',{done:false,dismissed_at:Date.now()})});addEventListener('resize',updateMode);
window.visualViewport?.addEventListener('resize',updateMode);
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;refreshInstallUi()});
window.addEventListener('appinstalled',()=>{deferredInstallPrompt=null;updateMode();S.toast('Athenaeum is als app geïnstalleerd.')});
addEventListener('athenaeum-sync',e=>{if(current()&&e.detail?.pid===current().id&&$('#syncStatus'))$('#syncStatus').textContent=syncStatusHtml(current())});if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register('./sw.js').catch(console.warn)}
init();
})();

