ATHENAEUM V1.2.0 — DEPLOYMENT NOTES

1. Upload this complete repository root to GitHub Pages.
2. Deploy the updated Supabase Edge Function: supabase/functions/telegram-bot/index.ts.
   This is required for personalized learning-path Telegram messages and profile_key-aware status/test calls.
3. No new database columns are required for learning paths: they live inside the profile-scoped paideia_state payload and sync through the existing state layer.
4. Existing Paideia profiles migrate automatically in the browser. Defaults preserve Ancient History + General History while exposing the new paths in Settings.
5. Clear/update the PWA once after deployment; cache names were bumped for Athenaeum V1.2.0 and Scriptorium V8.1.
6. Test at least: desktop browser, installed desktop PWA, phone portrait, tablet/iPad landscape, two different cloud profiles, and four concurrent sessions if available.

H5P
- Keep authoring outside Athenaeum (e.g. Lumi/H5P platform) to avoid loading the full H5P editor/library stack into the PWA.
- For automatic xAPI score/completion capture, publish the H5P web output under the same Athenaeum origin where possible.
- External embeds remain supported and isolated; completion can be stored manually when cross-origin isolation blocks xAPI access.
